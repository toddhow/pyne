datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model GuildSettings {
  id                String    @id
  disabledChannels  String[]
  modRoles          String[]
  prefixes          String[]
}